import "dotenv/config";

/** @type {import('prisma').PrismaClientOptions} */
const config = {
  // Add any Prisma configuration options here
  // See: https://www.prisma.io/docs/reference/api-reference/prisma-client-reference
};

export default config;
